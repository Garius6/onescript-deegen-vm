// Эталонный демо-скрипт для PoC onescript-vm.
// Запуск: osbc-export fib.os fib.osbin && osvm fib.osbin → 55

Функция Фиб(н)
    Если н < 2 Тогда
        Возврат н;
    КонецЕсли;
    Возврат Фиб(н - 1) + Фиб(н - 2);
КонецФункции

Сообщить(Фиб(10));
